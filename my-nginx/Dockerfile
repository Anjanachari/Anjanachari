# Dockerfile
FROM nginx:latest

# Optional: Add custom config or content
# COPY nginx.conf /etc/nginx/nginx.conf
# COPY html/ /usr/share/nginx/html/

# Optional label or maintainer
LABEL maintainer="you@example.com"
COPY html/ /usr/share/nginx/html/
